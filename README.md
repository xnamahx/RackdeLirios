# RackdeLirios

#### Installation

LIBUSB dependency

* ```git submodule update --init --recursive```

* ```./autogen.sh```

* ```./configure``` 

* ```make```

Create a plugin ZIP package.

* ```RACK_DIR=<path to Rack SDK> make dist```